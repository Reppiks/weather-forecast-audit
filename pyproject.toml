[project]
name = "weather-forecast-audit"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "streamlit>=1.64.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
